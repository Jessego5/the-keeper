.venv/
__pycache__/
*.pyc
.env
.DS_Store
memory_store/
*.db
backend/mcp.json
keeper_sandbox/
.keeper.log
DESIGN_NOTES.md
evals/results/
backend/notify/
memory_store_backup_*/

# Replay output. register_candidates.json is derived FROM memory_store/, which is
# ignored above precisely because conversation content is private, so tracking it
# would route real user messages around that rule. It is a working file for
# labelling; only the reviewed evals/register_dataset.json belongs in git.
evals/register_candidates.json

# Personal demo checklist. Working notes for a recording session, not part of the
# project; docs/DEMO.md is the version that belongs in git.
DEMO_TODO.md
